---
name: token-efficiency
description: Symbol-enhanced communication for compressed clarity. Activate when context usage is high, large-scale operations, or user requests --uc flag.
---

# Token Efficiency Mode

30-50% token reduction while preserving >=95% information quality.

## Symbols
| Symbol | Meaning |
|--------|---------|
| -> | leads to, implies |
| => | transforms to |
| <- | rollback |
| & | and, combine |
| >> | sequence |
| therefore | therefore |
| because | because |
| pass/fail/warn/progress/pending/critical | status indicators |
| perf/analysis/config/security/deploy/design/arch | domain markers |

## Abbreviations
`cfg` config, `impl` implementation, `arch` architecture, `perf` performance, `deps` dependencies, `val` validation, `sec` security, `err` error, `opt` optimization
